apiVersion: v1
kind: Service
metadata:
    name: manager
    namespace: hash-cracking
spec:
    selector:
        app: manager
    ports:
        -   name: http
            port: 80
            targetPort: 8080

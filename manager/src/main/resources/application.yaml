spring:
  application:
    name: manager

  mongodb:
    uri: mongodb://localhost:27017/hashdb

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080